using UnityEngine;
/**
public class Damage : MonoBehaviour
{
    public int DamageCalc(int character, int[] color){
        int damage = 0;
        for (int i = 0; i < 5; i ++){
            damage += this.Data.characters[character].attack * this.Data.characters[character].multiplier[i] * color[i];
        }

        return damage;
    }
}
**/